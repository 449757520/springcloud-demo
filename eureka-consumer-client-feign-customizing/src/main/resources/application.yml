server:
  port: 8283
spring:
  application:
    name: client-consumer-feign-customizing
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@localhost:8081/eureka #eureka服务地址
    healthcheck:
      enabled: true
  instance:
    prefer-ip-address: true      #显示ip
logging:
  level:
       com.example.eurekaconsumerclient.feign.TestClient: debug
#hystrix:
#  command:
#    default:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 5000      #解决超时问题(方案一：延长请求时间)
#hystrix:
#  command:
#    default:
#      execution:
#        timeout:
#          enable: fales       #解决超时问题（方案二：关闭请求时间限制）
feign:
  hystrix:
    enabled: false             #解决超时问题（方案三：关闭Feign的hystrix）